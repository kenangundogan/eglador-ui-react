{
  "name": "image-processing",
  "version": "1.0.0",
  "private": true,
  "description": "Sharp tabanlı sunucu taraflı görsel işleme — ImageCropper uploadEndpoint entegrasyonu",
  "scripts": {
    "build": "tsc --project tsconfig.json"
  },
  "dependencies": {
    "sharp": "^0.33.5"
  },
  "devDependencies": {
    "@types/multer": "^1.4.12",
    "@types/node": "^20.0.0",
    "typescript": "^5.0.0"
  },
  "peerDependencies": {
    "express": "^4 || ^5",
    "multer": "^1.4",
    "next": ">=14"
  },
  "peerDependenciesMeta": {
    "express": { "optional": true },
    "multer": { "optional": true },
    "next": { "optional": true }
  }
}
